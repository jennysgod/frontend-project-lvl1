name: linter

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest

  steps:
    - name: npm install
      run: 
        make install
    - name: Run Linter
      run: 
        make lint